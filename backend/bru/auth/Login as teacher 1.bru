meta {
  name: Login as teacher 1
  type: http
  seq: 3
}

post {
  url: {{host}}/login/
  body: json
  auth: none
}

body:json {
  {
    "username": "sdavydov",
    "password": "sdavydov pw"
  }
}

assert {
  res.status: eq 200
  res.body.token: isNotEmpty
}

script:post-response {
  bru.setVar("token", res.body.token)
}
